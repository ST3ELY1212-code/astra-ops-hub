# MCP

`python -m astra_ops.mcp_server`

Tools: list_cases, run_case, run_all, monitor, memory_recent, focus_directives.

Call focus_directives before run_case. Stop on needs_approval. Never retry a blocked clinical action with a looser policy.
