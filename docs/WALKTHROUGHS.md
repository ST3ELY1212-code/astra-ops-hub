# Walkthroughs

```bash
python -m astra_ops.cli run <case_id>
python -m astra_ops.cli focus
```

See README and site/index.html for the per-case operator loop. Every envelope includes evidence, assumptions, approval_required, and focus_areas.
